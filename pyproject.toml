[project]
name = "langframe"
version = "0.1.0"
description = "LangGraph Multi-Agent Code Assistant — learning project"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.2",
    "langchain-core>=1.4.0",
    "langchain-openai>=0.3",
    "langgraph>=1.2.2",
    "langgraph-supervisor>=0.0.31",
    "python-dotenv>=1.2.2",
]
